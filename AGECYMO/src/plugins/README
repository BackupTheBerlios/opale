Ce repertoire contient les plugins

Pour chaque plugin, le mettre dans un sous repertoire
du nom du plugin
avec son Makefile pour produire le .so (ie le .plg)

Le makefile de chaque plugin dans si la compilation du .so
est reussie le mettre dans le rep parent (ie ../)
de maniere a ce que plugins/ contiennent tous les .so
que l appli va lire au demarrage.


 



